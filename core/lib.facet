pub mod core {
  pub mod option {
    pub enum Option<T> {
      None,
      Some(affine T)
    }
  }

  pub mod result {
    pub enum Result<T, E> {
      Ok(affine T),
      Err(affine E)
    }
  }
}
