fn bad(mut x: unrestricted isize) -> unrestricted isize {
    let mut r: affine &mut unrestricted isize = (&mut x) in
    let mut rr: affine &mut affine &mut unrestricted isize = (&mut r) in
    let child: unrestricted &unrestricted isize = (&**rr) in
    (replace **rr 99)
}
