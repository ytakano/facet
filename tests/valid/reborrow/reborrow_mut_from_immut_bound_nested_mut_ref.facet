fn f(mut x: unrestricted isize) -> unrestricted () {
    let mut r: affine &mut unrestricted isize = (&mut x) in
    let rr: affine &mut affine &mut unrestricted isize = (&mut r) in
    let r2: affine &mut unrestricted isize = (&mut **rr) in
    (*r2 = 42)
}
