trait RunValue {
  fn run(self: Self) -> unrestricted isize;
}

impl RunValue for unrestricted isize {
  fn run(self: Self) -> unrestricted isize {
    self
  }
}

fn main() -> unrestricted isize {
  (RunValue::run 1)
}
