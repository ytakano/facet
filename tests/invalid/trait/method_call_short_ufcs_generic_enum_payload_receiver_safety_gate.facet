enum Job<T> {
  Ready(unrestricted T)
}

trait Runner {
  fn run(self: Self) -> ();
}

impl Runner for unrestricted Job<unrestricted isize> {
  fn run(self: Self) -> () {
    ()
  }
}

fn main() -> unrestricted () {
  (Runner::run Job<unrestricted isize>::Ready(1))
}
